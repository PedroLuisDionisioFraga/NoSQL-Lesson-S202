{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "_id",
      "title",
      "author",
      "year",
      "price"
    ],
    "properties": {
      "_id": {
        "bsonType": "int",
        "description": "must be an integer and is required"
      },
      "title": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "author": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "year": {
        "bsonType": "int",
        "description": "must be an integer and is required"
      },
      "price": {
        "bsonType": [
          "double",
          "decimal",
          "int"
        ],
        "description": "must be a floating-point or integer number and is required"
      }
    }
  }
}